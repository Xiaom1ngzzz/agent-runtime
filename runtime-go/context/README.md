# runtime-go/context

参考实现：上下文引擎。

对应章节：`chapters/ch03-context.md`。

计划内容：

- 消息序列的组装（system / few-shot / history / tool result）；
- 窗口裁剪策略（滑动窗口、摘要压缩、结构化截断）；
- Token 预算与成本控制；
- 与 State、Memory 模块的接口。

代码将在 Part II 落地。
